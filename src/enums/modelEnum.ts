export enum NotificationEnum {
  like = 'like',
  comment = 'comment',
  follow = 'follow',
}

export enum ToggleLikeEnum {
  liked = 'liked',
  unliked = 'unliked',
}

export enum ToggleFollowEnum {
  follow = 'followed',
  unfollow = 'unfollowed',
}
